package com.codagis.concorde.repository;

import com.codagis.concorde.domain.SoundboardBotSettings;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SoundboardBotSettingsRepository extends JpaRepository<SoundboardBotSettings, Long> {
}
